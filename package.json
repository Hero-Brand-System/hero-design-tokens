{
  "name": "hero-design-tokens",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "tsx scripts/validate-tokens.ts",
    "contrast": "tsx scripts/check-contrast.ts",
    "build:css": "tsx scripts/build-css.ts",
    "build:ts": "tsx scripts/build-ts.ts",
    "build:docs": "tsx scripts/build-docs.ts",
    "build": "npm run validate && npm run contrast && npm run build:css && npm run build:ts && npm run build:docs"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
